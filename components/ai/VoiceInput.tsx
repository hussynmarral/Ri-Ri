// Voice input placeholder — native voice recognition not available in this build.
// To re-enable: install @react-native-voice/voice, run expo prebuild, and restore the implementation.
export function VoiceInput(_props: { onResult: (transcript: string) => void; disabled?: boolean }) {
  return null;
}
